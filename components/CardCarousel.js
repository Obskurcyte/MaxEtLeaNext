import React from 'react';
import './CardCarousel.module.css';

const CardCarousel = ({number}) => {
  return (
    <div className="card-carousel">
      {number}
    </div>
  );
};

export default CardCarousel;
